general:
  branches:
    only:
      - react-rewrite
dependencies:
  pre:
    - rm -r ~/lcardito.github.io/node_modules/gh-pages/
deployment:
  production:
    branch: react-rewrite
    commands:
      - npm run deploy